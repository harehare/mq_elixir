rustler::atoms! {
    // Result atoms
    ok,
    error,

    // Result struct keys
    values,
    text,

    // Input format atoms
    markdown,
    mdx,
    html,
    raw,
    null,

    // Option keys
    input_format,
}
